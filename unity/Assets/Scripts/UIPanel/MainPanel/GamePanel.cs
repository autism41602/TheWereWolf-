using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GamePanel : BasePanel
{
    public GamePanel(string packageName, UIPanelType uiPanelType, UIManager uiManager) : base(packageName, uiPanelType, uiManager)
    {
    }
}